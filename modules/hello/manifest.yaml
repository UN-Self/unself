# SPDX-License-Identifier: AGPL-3.0-only
# 模块契约 v1（docs/modules.md §3 字段冻结；决策 #56/#57）
# entry 为占位：M0 后由装配器覆写为实例内地址（§3）
id: hello
route: /m/hello
entry: http://localhost:8790/
runtimes:
  - worker
version: 0.1.0
description: hello world 演示模块（SDK 存储计数器）
# 门禁词表见 @unself/contracts MODULE_PERMISSIONS；hello 只经 SDK 存储读写
permissions:
  - storage
# storage 省略 = 只支持 core（数据经 Core API 代理，#55）
