# 依赖
node_modules/

# 构建产物
dist/
coverage/

# Cloudflare Wrangler 本地状态
.wrangler/

# 部署脚本生成的装配产物（临时部署配置与 shell 构建副本）
.deploy/

# 安装器 tarball（#257：npm pack 产物，可重建——交付走 PR 附件/发布流水线，不入库）
packages/installer/*.tgz

# 环境变量与密钥（pnpm-lock.yaml 必须入库，勿忽略）
.dev.vars
.env
.env.*

# 本地临时/个性化文件
*.local

# 系统与编辑器
.DS_Store
.idea/
.vscode/

# unself.lock（#245）：实例侧生成物——样例实例纳入版本管理，仓库根开发实例不落盘
/unself.lock

# SDK tarball（可重建，不入库）
packages/sdk/*.tgz
