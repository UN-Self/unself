# 产品需求文档（PRD）：Unself P2P 虚拟组网系统

> 文档状态：需求基线 v0.3
> 适用范围：第一版 MVP
> 更新日期：2026-07-28

## 1. 产品目标

Unself 是面向开发者、小团队和个人自托管场景的三层虚拟组网工具。系统基于 Linux TUN 与 Iroh，在节点之间建立端到端加密连接：优先使用 P2P 直连，打洞失败时由用户自建的公网节点提供 Relay 转发。

产品不追求“完全不需要公网基础设施”。现实网络中的家庭 NAS、家庭服务器通常没有公网 IP，因此公网节点是系统稳定性的关键组成部分。Unself 的目标是消除单一主控和固定 Relay 依赖，让多个公网节点多活、动态选择，并在全部 Relay 丢失后通过可替换的发现渠道恢复网络。

第一版的核心体验是：安装了 Unself 的 Linux 设备能够像处于同一个三层私网中一样，使用稳定虚拟 IP 访问其他节点上的任意 TCP/UDP 服务。

## 2. 设计原则

1. **同一程序、能力开关：** 不提供独立的 Edge 端和 Relay 端。所有节点运行同一个二进制，是否提供中继由 `relay.enabled` 显式控制。
2. **P2P 优先、Relay 兜底：** 能直连时不经过 Relay；不能直连时选择当前质量最好的 Relay。
3. **无主多活：** Relay 不选主、不保存权威全局状态。每个节点独立探测和选择路径。
4. **身份与位置分离：** 节点身份、Iroh 通信身份、当前 IP/端口不是同一概念；网络位置变化不改变逻辑身份。
5. **私网路由最小化：** 只接管 Unself 虚拟网段，不修改默认路由、不接管 DNS、不充当全局代理。
6. **故障后可重建：** 在线路由表是动态缓存，不依赖某台 Relay 永久保存。
7. **明确现实边界：** 不承诺 100% 打洞成功、零丢包切换或所有既有 TCP 会话无感迁移。

## 3. 第一版范围

### 3.1 支持范围

- 平台：Linux。
- 运行方式：原生二进制与 Docker 镜像。
- Docker 网络：`host` 模式，不使用 Docker 端口映射。
- 虚拟网络：IPv4 三层 TUN。
- 默认网段：`100.127.0.0/16`。
- 组网模型：一个进程只加入一个 Unself 网络。
- 访问模型：同一网络内默认全互通，不提供 ACL。
- 节点规模基线：最多 100 个普通节点、最多 10 个 Relay 能力节点。

### 3.2 第一版不包含

- Windows、macOS 常驻节点；未来可作为临时 VPN 式接入客户端。
- Web 管理面板、用户系统、节点级允许/拒绝名单和权限管理。
- 内置 DNS、名称解析或以名称作为路由主键。
- 默认路由接管、出口节点、全局代理或 DNS 劫持。
- 子网路由器；不代理访问节点所在物理局域网中的其他设备。
- 二层以太网、ARP、mDNS、Bonjour、NetBIOS、广播和组播转发。
- 多网络同时加入。
- 对既有 TCP/SSH/文件传输会话的无损跨 Relay 迁移保证。

## 4. 节点、身份与准入

### 4.1 统一节点模型

所有实例均为 `Node`，具备 TUN、P2P、路由同步和服务承载能力。公网 VPS 可显式开启：

```toml
[relay]
enabled = true
```

开启后，该节点在普通节点能力之上提供 Iroh Relay 转发。Relay 节点本身也拥有虚拟 IP，可以在 Unself 私网中运行和访问服务。

系统不得仅凭检测到公网 IP 自动开启 Relay，以避免未经用户确认产生带宽、费用和暴露面。

### 4.2 身份分层

- **Unself NodeID：** 网络中的稳定逻辑主键。
- **Iroh EndpointID：** 当前实例的密码学通信身份，由本机 Iroh 私钥生成。
- **Virtual IP：** 三层路由地址，可配置、可迁移，不作为身份主键。
- **Name：** 可随时修改的显示名称，不参与认证和路由。

首次初始化生成并持久化 Unself NodeID、Iroh 私钥与身份版本。配置和业务状态必须与私钥文件分离。

### 4.3 网络准入

第一版使用可替换的认证提供器模型，内置实现为共享密钥：

```toml
[auth]
provider = "shared-secret"
secret_file = "/var/lib/unself/cluster.secret"
```

共享密钥用于网络准入、恢复公告加密和控制消息鉴权。Iroh 负责端到端加密和 EndpointID 验证，Unself 在握手后验证对方是否属于当前网络。

第一版默认网络成员彼此信任。任何持有共享密钥的成员均可加入网络、开启 Relay，并执行节点替换。后续版本可替换为管理密钥、用户选择的认证方式或节点级权限。

### 4.4 新节点加入

第一版使用 `join-token` 加入网络。`join-token` 是入网授权凭证，持有者可以把新节点加入当前 Unself 网络，因此必须按密码或部署密钥管理；泄露意味着当前网络准入边界失效。

为了兼顾灾难恢复和日常部署，产品模型将恢复信息与入网授权分离：

- **Recovery Bundle：** 包含虚拟网段、协议版本、发现后端配置、网络发现标识和必要的引导地址信息；它帮助新节点找到网络入口，但不授予入网资格。
- **Join Token：** 包含或可验证入网授权，允许节点完成 Unself 网络认证；它是真正的入网钥匙。

CLI 可以提供合并形态以降低首次体验成本，但实现和文档必须明确区分“找路”和“开门”两类信息。Cloudflare、DHT 等恢复后端只参与发现入口，不承担成员审批，也不保存入网密钥。

## 5. 虚拟网络与 TUN

### 5.1 路由边界

Unself 创建 `unself0` TUN，并只添加 `100.127.0.0/16` 路由。系统不得：

- 修改系统默认路由；
- 修改系统 DNS；
- 代理非 Unself 网段流量；
- 影响用户原有互联网、局域网或代理软件路由。

默认 MTU 为 1280，可由高级配置覆盖。该值用于降低 QUIC/UDP 封装后发生外层分片的概率。

### 5.2 虚拟 IP

新节点默认自动分配虚拟 IP；生产使用也允许显式配置虚拟 IP：

```toml
[network]
virtual_ip = "100.127.0.10"
```

自动分配根据当前已知路由表选择空闲地址，并在成功后持久化。用户可以通过配置文件或加入参数显式指定地址。自动分配仅为尽力而为，不提供分区环境下的强一致分配保证。

虚拟 IP 绑定记录必须包含可比较的分配时间或等价版本信息。若发现两个在线 NodeID 声明同一虚拟 IP，较早获得该 IP 的节点保持不变；后加入或后分配的一方停止使用冲突 IP，自动选择新的空闲 IP，并发布新的节点记录。若分配时间无法比较，则使用 NodeID 等稳定规则裁决，避免两边反复变更。

显式节点替换不按“新加入节点”处理。`--replace-node` 继承旧逻辑节点的虚拟 IP、分配时间和身份版本规则，替换实例应保留原 IP 并顶掉旧实例。

### 5.3 私网端口与防火墙

Unself 不限制虚拟私网内的 TCP/UDP 端口。目标服务只要监听虚拟 IP 或 `0.0.0.0`，即可通过 `100.127.x.x:port` 访问。

默认仅为 `unself0` 安装一条允许规则：允许来源属于 `100.127.0.0/16` 的入站流量。系统不得放宽公网网卡规则，不得清空用户防火墙；退出时应清理自己创建的规则。该行为可关闭。

## 6. 路由记录与在线状态

### 6.1 节点记录

节点发布带版本和签名的动态记录，至少包含：

```text
Unself NodeID
Iroh EndpointID
identity_version
virtual_ip
display_name
direct_addresses
reachable_relays
relay_capability
record_sequence
issued_at / expires_at
signature
```

Iroh ticket 只用于首次引导或人工恢复。长期保存的核心是稳定 NodeID、EndpointID 和持续更新的节点记录，因为 ticket 中的直连地址与 Relay 地址可能过期。

### 6.2 状态机

路由记录使用以下状态：

- `learned`：从其他节点缓存或恢复摘要获得，尚未验证。
- `probing`：正在尝试建立连接。
- `online`：已完成直接握手并持续收到心跳，可参与路由。
- `suspect`：连续心跳缺失，暂不作为优选路径。
- `offline`：超过离线阈值，从在线路由表移除。
- `expired`：超过缓存 TTL，删除动态记录。

建议默认值：心跳间隔 10 秒，连续缺失 3 次进入 `suspect`，60 秒无有效心跳进入 `offline`，动态缓存保留 24 小时。

只有直接握手或心跳确认的节点才能进入在线路由表。其他节点转交的缓存仅用于加速恢复，不能直接证明在线。

### 6.3 路由同步

Relay 维护当前在线和近期在线的路由缓存，不永久保存权威表。节点通过所有健康 Relay 维持轻量控制连接并注册自身状态；已认证的现有邻居连接也可以交换签名记录摘要，帮助 Relay 重启和网络恢复。

新连接节点从任意 Relay 或邻居获取当前记录摘要，再按版本拉取缺失记录。各节点最终收敛到相同的在线视图，但短暂网络分区期间允许存在差异。

Relay 重启后路由表可以为空。节点重新注册，邻居提供缓存摘要，Relay 将这些记录先标记为 `learned`，再通过后续握手和心跳恢复在线表。

普通离线不产生全局删除操作。节点超过 TTL 后自然遗忘；本地 `forget` 仅清理本机缓存，不等同于撤销网络访问权。

## 7. P2P、Relay 与负载均衡

### 7.1 路径建立

节点访问目标虚拟 IP 时：

1. 查询虚拟 IP 对应的当前 NodeID 与 EndpointID。
2. 使用 Iroh 尝试直连和打洞。
3. 直连成功后迁移到 P2P 路径。
4. 直连失败或质量不可用时，通过 Relay 转发。

Relay 只转发端到端加密数据，不作为虚拟 IP 路由的永久主控。

### 7.2 多 Relay

所有 Relay 多活，不设置主从或选主。节点对每个 Relay 持续收集：

- RTT 与抖动；
- 丢包和连接成功率；
- Relay 报告的负载与资源状态；
- 最近失败次数和可用性。

节点独立计算评分，选择一个 `active_relay` 承载当前新增的兜底连接，其他节点作为 `standby_relays`。切换必须设置阈值、持续观察窗口和冷却时间，避免因短时波动频繁抖动。

第一版只承诺连接级切换：新连接和可重新建立的流量切换至更优 Relay。已经建立的 TCP/SSH/文件传输可能因底层连接中断而重连，不能宣传为零丢包或完全无感。

### 7.3 传输监听

Docker 使用 Host 网络，因此不配置 Docker `ports` 映射。以下仅为进程默认监听和公网安全组的建议最小集合，均可配置：

```text
443/TCP   Iroh Relay 可靠转发
7824/UDP  Unself/Iroh Endpoint 直连
7842/UDP  Iroh QAD 地址探测
```

普通家庭节点不要求公网端口转发，只需允许主动出站；其局域网和 TUN 内部端口不受 Unself 限制。

Relay 不要求域名。它通过加密签名公告发布当前 IP、端口、Iroh 身份和必要的传输证书信息；地址变化后发布新记录。证书生成与验证属于内部实现，不暴露为第一版用户配置。

## 8. 灾难恢复与发现

### 8.1 恢复目标

正常情况下依靠 P2P、邻居缓存和多个 Relay 维持网络。最后一个 Relay 消失后，部署新公网节点应最多只需要一次人工操作，不能要求逐个登录旧节点。

不存在任何共同发现渠道且旧网络已经物理分区时，无法保证新节点凭空发现所有分区。Unself 通过多个可替换的发现后端降低该风险，但必须在文档中保留这一边界。

### 8.2 Relay 公告

只有显式启用 Relay 的节点向灾难发现后端发布短期公告。公告至少包含：

```text
network_discovery_id
Unself NodeID / Iroh EndpointID
identity_version
EndpointAddr
Relay 当前 IP 与端口
传输证书指纹或等价验证信息
issued_at / expires_at
signature
```

公告在本地签名并加密。`network_discovery_id`、加密密钥和发布鉴权密钥分别从共享密钥派生，发现服务不能解密路由信息，也不能获得原始共享密钥。

灾难发现后端只保存 Relay 公告，不保存普通节点、完整路由表或业务流量。

### 8.3 发现后端

第一版支持可插拔、多来源发现：

1. **Peer cache / ticket：** 使用本地缓存和人工提供的 Iroh ticket。
2. **公共 DHT：** 保存加密、签名、短期有效的 Relay 公告，不承载业务流量。
3. **HTTP Recovery Backend：** 通用 HTTP 接口；推荐参考实现为 Cloudflare Worker + D1。

Cloudflare 后端是可选公告箱，不是主控。它按网络发现标识和 Relay NodeID 保存密文与过期时间，不保存共享密钥。允许配置多个 HTTP 后端，任意一个可用即可恢复。

Relay 地址不依赖 DNS。Cloudflare `workers.dev` 或自定义 HTTP 后端 URL 仅是灾难公告渠道，不参与节点间虚拟 IP 路由。

### 8.4 自动恢复流程

1. 新 VPS 使用原网络共享密钥启动，并显式开启 Relay。
2. 新节点生成身份与当前 EndpointAddr，向 DHT 和 HTTP 后端发布 Relay 公告。
3. 没有健康 Relay 的旧节点进入恢复模式，查询所有发现后端。
4. 旧节点验证、解密公告，并主动连接新公网节点。
5. 完成 Iroh 与 Unself 网络认证后，双方交换节点记录摘要。
6. 新 Relay 将缓存记录标记为 `learned`，随着各节点回连重新确认在线状态。
7. 其他节点动态加入新 Relay 并重新选择路径，在线路由表逐步收敛。

建议恢复查询退避：立即、5 秒、15 秒、30 秒、60 秒，随后每 5 分钟；恢复成功后每 15 分钟低频检查。Relay 公告建议有效期 2 小时，每 30 分钟刷新，并加入随机抖动。

### 8.5 人工 ticket 兜底

当公共 DHT 和 HTTP 后端均不可用时：

```text
新节点主动拉取：unself join --peer-ticket <旧节点 ticket>
旧节点反向引入：unself introduce <新节点 ticket>
```

优先由新公网节点联系一个仍可达的旧节点并拉取缓存；若旧节点位于无法主动打入的 NAT 后，则由旧节点主动连接新公网节点。只需在一个仍与其他节点连通的旧节点操作一次，信息即可继续传播。

若旧网络已分裂为多个互不相通的部分，且所有公共发现渠道均不可用，则只能恢复被引入节点所在的连通部分。

## 9. 节点替换

### 9.1 语义

普通初始化表示新增节点。显式替换表示新物理实例立即接管一个已有 Unself NodeID：

```text
unself init --replace-node <旧 Unself NodeID>
```

替换是一次性事务，不是节点长期携带的 `replace` 字段。

### 9.2 执行规则

1. 新 VPS 生成新的 Iroh 私钥和 EndpointID。
2. 发布经过共享密钥授权的替换提交。
3. 目标 Unself NodeID 立即绑定新的 EndpointID，并提升 `identity_version`。
4. 虚拟 IP、显示名称等逻辑节点属性转给新实例。
5. 旧 EndpointID 的现有控制和数据连接被关闭。
6. 旧实例进入 `superseded` 状态，停止组网；必须人工重新初始化才能作为新节点加入。
7. 新实例持久化最终身份后消费掉替换启动参数，后续重启不重复执行。

替换不等待旧节点离线，不需要旧节点批准。分区或离线节点重新接入时，以更高的身份版本为准并丢弃旧绑定。

第一版共享密钥模型下，任何持有共享密钥的成员都能替换任意 NodeID。这是当前信任边界，不等同于安全吊销；若旧 VPS 或共享密钥疑似泄漏，应更换网络共享密钥。后续管理体系再提供细粒度替换和吊销权限。

## 10. 部署配置

### 10.1 Linux 原生

原生二进制推荐由 systemd 管理。创建 TUN、路由和窄范围防火墙规则需要 `CAP_NET_ADMIN`；数据封装、Iroh 连接和路由决策均在用户态完成。

### 10.2 Docker

Docker 作为第一版正式支持的部署方式：

```yaml
services:
  unself:
    image: ghcr.io/un-self/unself:latest
    network_mode: host
    cap_add:
      - NET_ADMIN
    devices:
      - /dev/net/tun:/dev/net/tun
    volumes:
      - ./unself-data:/var/lib/unself
    restart: unless-stopped
```

Host 网络模式使容器与宿主机共享网络命名空间，不需要端口映射。它不会自动绕过宿主机防火墙；Unself 只管理自己在 `unself0` 上创建的规则。

### 10.3 配置示例

```toml
[node]
name = "home-nas"
state_dir = "/var/lib/unself"

[auth]
provider = "shared-secret"
secret_file = "/var/lib/unself/cluster.secret"

[network]
cidr = "100.127.0.0/16"
virtual_ip = "100.127.0.10"
tun_name = "unself0"
tun_mtu = 1280
manage_firewall = true

[relay]
enabled = false
relay_tcp_port = 443
endpoint_udp_port = 7824
qad_udp_port = 7842

[[discovery]]
type = "dht"
enabled = true

[[discovery]]
type = "http"
url = "https://example.workers.dev"
```

配置中的显示名称可以修改；NodeID 不由名称生成。Relay 地址、节点 EndpointAddr 和在线路由表属于动态状态，不要求用户手工长期维护静态种子列表。

## 11. 安全边界

- 节点间数据必须使用 Iroh/QUIC 提供的端到端认证加密。
- Relay 只能看到转发所需的连接元数据和加密载荷，不能解密 TUN 内业务数据。
- 公共 DHT 和 HTTP 恢复后端只能保存短期密文公告。
- 不得使用“跳过证书验证”作为生产默认配置。
- Docker Host 网络加 `NET_ADMIN` 权限较高，镜像应最小化并固定依赖版本。
- 第一版无成员级权限隔离；共享密钥泄漏意味着整个网络准入边界失效。

## 12. 性能与可靠性边界

- P2P 吞吐受双方带宽、NAT、CPU、MTU、拥塞控制和 Iroh 实现影响，不承诺固定达到物理带宽的某一比例。
- Relay 吞吐受 VPS 带宽、连接数、限速和跨运营商质量影响。
- Relay 切换是连接级恢复，不保证既有上层 TCP 会话不断开。
- 对称 NAT、严格防火墙、UDP 全阻断和运营商策略可能使 P2P 失败；TCP Relay 提供兜底但不宣传百分之百连通。
- 公共 DHT 在部分网络中可能不可达；Cloudflare 等 HTTP 后端也可能受网络或供应商故障影响，因此必须支持多后端和人工 ticket。

## 13. MVP 验收标准

1. 两个 Linux 节点通过 `100.127.0.0/16` 虚拟 IP 完成双向 TCP/UDP 单播通信。
2. 启停 Unself 不改变默认路由和 DNS，原互联网与局域网访问保持正常。
3. 能观察并区分 P2P 直连、打洞中和 Relay 转发状态。
4. 至少两个 Relay 同时在线时，节点能根据动态质量选择路径；一个 Relay 下线后，新连接自动切换。
5. Relay 重启且内存路由表为空时，能由节点注册和缓存摘要重建在线表。
6. 全部旧 Relay 消失后，新 Relay 通过 DHT 或 HTTP 恢复公告使旧节点自动回连，不要求逐台操作。
7. 公共发现不可用时，能通过一次 ticket 引入恢复仍然连通的网络部分。
8. `--replace-node` 能立即更新逻辑 NodeID 到新 EndpointID 的绑定，并使旧实例进入 `superseded`。
9. Docker Host 模式能够创建 TUN、安装和清理私网路由及防火墙规则，并持久化节点状态。
10. 虚拟私网内不设置应用端口限制；未安装 Unself 的物理局域网设备不会被自动代理。

## 14. 后续方向

- Windows 与 macOS 临时接入客户端。
- Web 管理面板、用户体系、节点审批、吊销和 ACL。
- 可替换认证提供器和管理密钥。
- 子网路由、出口节点和按需访问策略。
- DNS/名称发现、广播和组播兼容层。
- 更完整的 Relay 资源上报、限速、计费保护和容量调度。
- 跨连接的平滑迁移能力；仅在底层协议可验证支持时提供相应承诺。
