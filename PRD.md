# 产品需求文档 (PRD)：Iroh-Tunnel 智能 P2P 虚拟组网系统

## 1. 文档概述

### 1.1 目的

本文档旨在定义 **Iroh-Tunnel** 系统的产品需求与技术架构规格。该系统旨在通过应用层 P2P 打洞技术与用户态虚拟网卡（TUN）深度结合，构建一个**去中心化、免中心化服务器、具备多Server容灾与智能IP自适应分配能力**的三层（网络层）虚拟专用网络系统。

### 1.2 背景与术语定义

* **Iroh / noq：** 基于 Rust 开发的高性能、模块化 P2P 网络协议栈与自定义 QUIC 实现。
* **TUN (Network Tunnel)：** 操作系统内核提供的虚拟三层网络设备，通过对其读写可以截获或注入原始 IP 数据包。
* **NodeID：** 基于 ED25519 非对称加密算法生成的节点全球唯一公钥标识，作为 P2P 通信的唯一拨号地址。
* **DERP / Relay：** 处于恶劣 NAT 环境下，打洞失败时用于双向中转流量的加密中继协议/服务。
* **Gossip：** 节点间无中心、通过反熵和去中心化发布/订阅机制传播元数据的广播协议。

---

## 2. 角色与业务模型设计

系统彻底模糊传统的“客户端/服务端”物理边界，全网节点在网络拓扑上皆为对等 Peer。通过**能力开关**将节点行为抽象为两种配置角色：

### 2.1 基础对等节点 (Edge Peer)

* **典型设备：** 家用 NAS、本地开发机、受限沙箱容器。
* **核心行为：** 启动本地 TUN 网卡，通过 NodeID 寻址与其他对等节点建立 P2P 直连通道；不承担外部节点的中继转发义务。

### 2.2 中继对等节点 (Relay Peer / Server)

* **典型设备：** 具有公网静态 IP 的云服务器 (VPS)。
* **核心行为：** 在具备 Edge Peer 所有组网能力的基础上，额外向全网/私有集群开放中继服务（`iroh-relay`）。当集群内其他 Edge Peer 之间遭遇恶劣 NAT 导致打洞失败时，自动承载流量中转。支持**多 Server 部署，实现分布式负载均衡与动态容灾**。

---

## 3. 核心功能需求 (Functional Requirements)

### 3.1 去中心化自适应智能 IP 分配机制 (Decentralized Auto-DHCP)

为了彻底摆脱中央控制器的逻辑绑定，系统必须支持免配置的智能 IP 分配：

1. **哈希映射：** 节点首次启动若未检测到静态 IP 缓存，必须自动对自身的 `NodeID` 进行确定性哈希计算，映射到预设的私有网段（如 `10.0.0.0/24`）中：

$$\text{IP\_Suffix} = \text{Hash}(\text{NodeID}) \pmod{254} + 1$$


2. **地址冲突检测 (ACD)：** 在正式绑定网卡前，节点必须通过内置的 `iroh-gossip` 协议向当前在线的已知 Peer 发送内网 IP 占用宣告包。
3. **冲突自愈：** 若收到其他节点回传的“已被占用”反对包，当前节点必须将原哈希值执行单调递增变异（如 `+1`），并重新发起宣告，直至网络无冲突响应。
4. **静态固化：** 确定可用 IP 后，节点必须将此 IP 持久化写入本地状态配置文件，使其退化为静态 IP，后续重启优先申请此 IP。
5. **全网收敛：** 绑定成功后，节点发布 `[NodeID] <---> [Virtual IP]` 的最终映射关系，全网 Peer 增量更新本地内存路由表。

### 3.2 多中继弹性负载均衡与感知切换 (Multi-Relay Failover)

1. **并行长连接维持：** 节点启动时，必须根据配置文件中指定的“种子服务器列表”，同时与多个处于公网的中继节点（Server）建立控制面连接。
2. **最优路径动态评级 (Path Selection)：** 底层 noq 传输层必须实时监测各条中继链路的丢包率、拥塞窗口以及 RTT（往返时延）。在 P2P 通道尚未打通或打洞宣告失败时，应用层流量必须被自动投递至当前综合权值最优（如 RTT 最短）的中继节点上。
3. **零感容灾退化：** 当作为当前主中继的 Server 节点发生异常断线时，noq 协议栈必须在内核态/用户态完成秒级无损包重调度，将待发数据无缝向备用 Server 迁移，上层虚拟网卡持有的标准 TCP 连接不发生断开。

### 3.3 物理层大包分片优化管理 (MTU Adaptation)

1. **网卡 MTU 硬性约束：** 为了防止三层 IP 数据包在套上应用层 QUIC 头部、UDP 头部及物理 IP 头部后超过广域网标准物理 MTU（1500 字节）导致严重的链路丢包，系统在调用操作系统底层 API 创建虚拟网卡（TUN）时，**必须将网卡自身的 MTU 硬性约束为 1280 字节**（IPv6 最低安全标准 MTU）。
2. **内核侧自动整形：** 依赖操作系统内核的 TCP/IP 协议栈自动根据 1280 字节进行应用层大包（如 OpenResty 转发来的 HTTP Body 字节流）的分片和整形。

---

## 4. 非功能性需求与性能指标 (Non-Functional Requirements)

### 4.1 性能与吞吐率

* **直连吞吐效率：** 在打洞成功（P2P 直连模式）下，大文件传输或 RPC 调用的系统吞吐量必须达到当前物理网络对称带宽上限（排除硬件加密损耗）的 **85% 以上**。
* **内存垃圾回收抖动：** 整个隧道数据流流转系统必须完全运行在无 GC（垃圾回收）暂停的运行时环境下（基于纯 Rust + Tokio），避免在大流量场景下产生内存分配器调度导致的延迟崩溃。

### 4.2 安全性要求

* **主权加密：** 集群内部网络报文交互必须全面基于 **TLS 1.3** 与 **ED25519 密钥对** 进行端到端加密防护。任何第三方或非集群内授权 NodeID 的节点流量，在中继节点或边缘节点层必须被直接断开。
* **零特权安全边界：** 除在创建/初始化系统 TUN 虚拟网卡时需要申请操作系统的网络管理特权外，核心数据路由、P2P 状态机打洞、以及内存解封装必须完全隔离在用户空间（User-space）完成，禁止向内核层注入未审查逻辑。

---

## 5. 核心数据结构与接口设计说明 (Technical Spec)

### 5.1 内存核心路由表结构

系统在内存中通过高度并发安全的读写锁维护一张全局路由表，用于将操作系统的三层流量精准分发至 Iroh 的对等连接中：

```rust
// 核心路由映射表设计参考
pub struct RoutingTable {
    // 通过虚拟内网 IP 精准查找目标的 Iroh 密码学公钥
    pub ip_to_node: Arc<RwLock<HashMap<Ipv4Addr, iroh::NodeId>>>,
    // 维持激活状态的 Iroh 物理连接池，平摊连接握手时延
    pub active_connections: Arc<RwLock<HashMap<iroh::NodeId, iroh::Connection>>>,
}

```

### 5.2 核心业务配置文件样例 (`config.toml`)

```toml
[node]
cluster_id = "cluster-secure-uuid-99a1-bc" # 集群全局鉴权口令
virtual_ip = "auto"                        # 支持 "auto" 自适应哈希或指定静态 IP
virtual_netmask = "255.255.255.0"
tun_interface_name = "itun0"               # 虚拟网卡名称
tun_mtu = 1280                             # 规避缓冲区膨胀与大包分片

[relay_capability]
enable_relay_service = true                # 核心开关：若部署在公网 VPS 则设为 true 激活Server角色
relay_bind_port = 7824                     # 自建中继端口

[[static_seeds]]
# 配置多 Server 负载均衡与容灾的种子中继节点列表
[static_seeds.server1]
node_id = "ae5b3f2c..."
endpoint = "1.2.3.4:7824"

[static_seeds.server2]
node_id = "cd7e9f1a..."
endpoint = "5.6.7.8:7824"

```

---

## 6. 系统局限性与风险控制策略

* **网络极其恶劣情况下的退化风险：** 若双端节点均处于防火墙规则极其严格的对称 NAT 之后且禁止任何外部 UDP 探测，系统将彻底丧失 P2P 直连能力。
* *规避方案：* 产品在设计上依赖多自建 Server 中继进行 WebSocket 443/7824 端口兜底。只要能访问网页，就能退化为“frp转发模式”百分之百保证业务连通性。


* **嵌套拥塞控制（TCP-over-QUIC）造成的速度悬崖风险：**
* *规避方案：* 锁定外层 noq 协议栈原生采用 **BBR 拥塞控制算法**。BBR 算法不将瞬时物理丢包视作拥塞发生器，可以极大地抚平与内层应用级 TCP（如 Linux CUBIC 算法）的重传冲突，避免由于重传时差导致的带宽断崖下降。
