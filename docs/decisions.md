# 决策档案（编号 + 日期 + 结论 + 来源；只增不改）

> 本文件是 Unself 全部拍板决策的唯一去处。规则：**只增不改**——已编号条目的结论不重写；更正与补充另开新条目（或新编号）。
> 编号沿革：`#1–#34` 沿用 docs/requirements.md 原「已确认架构决策」表编号（2026-09-06 表；表内个别条目带后续修订日期，见「日期」列）。**原表存在两个 `#31`（发信定位 / 模块三分法），本文件保留原编号、不重排**；新增条目自 `#35` 起顺延。
> 来源：`docs/requirements.md` 原 §6（2026-09-06）；`docs/PRODUCT_SPEC.md` v3 §6.6 M1 产品化决策记录（2026-09-07）；散落各章的拍板条目（各自标注日期与出处）。文中「SPEC §x」均指拆分前的 PRODUCT_SPEC v3 原文（现索引页见 docs/PRODUCT_SPEC.md）。
> 「结论」列保留原文措辞；原文含理由的随文保留，不另行补写。决策正文的展开位置：定位与产品 → docs/product.md；架构与契约 → docs/architecture.md；链路 → docs/flows.md；里程碑 → docs/roadmap.md。

| # | 日期 | 决策 | 结论 | 来源 |
|---|---|---|---|---|
| #1 | 2026-09-06 | 产品形态 | 自托管软件，不提供中央 SaaS 或多租户托管平台 | requirements.md §6 #1 |
| #2 | 2026-09-10 | 首选部署 | Cloudflare Workers（按量计费）；正式路径 = 交互式装配器 CLI；一键入口（Deploy Button + Workers Builds）为路线图（2026-09-10 修订） | requirements.md §6 #2 |
| #3 | 2026-09-06 | Docker | 作为替代部署和扩展能力，不与 CF 主路径竞争 | requirements.md §6 #3 |
| #4 | 2026-09-10 | 身份 | 登录能力核心内建：内置账号（用户名+密码）为默认；外部 OIDC 为可选增强；支持任意兼容身份源或支持 OIDC 的邮局；身份不是模块（2026-09-10 修订） | requirements.md §6 #4 |
| #5 | 2026-09-06 | 模块 | 聊天、日历、文档、看板、会议、Git、邮件均可选；未启用则不显示、不部署 | requirements.md §6 #5 |
| #6 | 2026-09-06 | 即时通讯 | EdgeChat 是官方 Cloudflare 模块，使用 Worker + Durable Objects；可在未来接入其他实现 | requirements.md §6 #6 |
| #7 | 2026-09-06 | 会议 | Cloudflare Worker + DO 做信令，浏览器 WebRTC P2P 传媒体；TURN/SFU/录制作为外部扩展 | requirements.md §6 #7 |
| #8 | 2026-09-06 | 日历与邮件 | CalDAV/iCalendar 与 SMTP 为通用协议；Stalwart 是官方参考适配器 | requirements.md §6 #8 |
| #9 | 2026-09-06 | 代码组织 | 单一 monorepo 管理核心、模块、适配器和部署；第三方完整源码保持独立构建边界 | requirements.md §6 #9 |
| #10 | 2026-09-06 | 许可证 | **核心 = AGPL-3.0**（防 SaaS 白嫖，威胁模型明确）；EdgeChat 衍生件遵守 GPL-3.0（独立 Worker）；重写的 EdgeChat 前端为自研归 AGPL；MiroTalk 衍生件遵守 AGPL-3.0；边界靠构建产物判定，LICENSE/NOTICE/components.yaml 落仓库；贡献用 DCO | requirements.md §6 #10 |
| #11 | 2026-09-06 | 模块装载 | 模块为自包含部署单元；默认 iframe 装载器，第一方同域路径挂载，微前端不采用；契约不写死 iframe | requirements.md §6 #11 |
| #12 | 2026-09-06 | 模块身份 | 核心统一身份（内置或 OIDC）并签发模块 token；模块只验 token，不接触身份源 | requirements.md §6 #12 |
| #13 | 2026-09-06 | 域名 | 单域名路径制 `/m/<模块>/`；Workers Routes 与 openresty location 等价；第三方模块可用独立域名 entry | requirements.md §6 #13 |
| #14 | 2026-09-06 | 数据归属 | 两个 D1：core（平台数据，物理独立）+ modules（全部模块业务数据，表前缀=模块id）；R2 统一适配层按前缀隔离；export/purge 为模块生命周期契约；模块一律经 SDK 存储接口访问，禁止跨模块查询与外键 | requirements.md §6 #14 |
| #15 | 2026-09-10 | 部署编排 | 装配 = wrangler 幂等脚本读 `unself.config.jsonc`，只在部署时执行，实例不持 CF 凭证；装配器是产品核心交互（token 深链接→三选→九步→错误归属）；Deploy Button + Workers Builds 为路线图一键入口；已部署模块启停 = 注册表开关 + token 门禁，秒级免部署；Docker 读同一份配置（2026-09-10 修订） | requirements.md §6 #15 |
| #16 | 2026-09-06 | M0 技术栈 | TypeScript + Hono + Vue 3 + Vite + Tailwind + zod + jose + pnpm workspaces + Vitest；M0 = hello 模块七步验收剧本 | requirements.md §6 #16 |
| #17 | 2026-09-06 | 会议记录 | 音频存档永不上云；转写两档（云/机密强制本地），主持人选定、成员可 opt-out；房间时钟统一时间轴，散场只传纯文字由 Worker 合并；核心代调 act claim + 通用 ACL（参会者读、主持人写）；AI 纪要经 SDK 能力；个人片段参会者互见 | requirements.md §6 #17 |
| #18 | 2026-09-06 | 前端约定 | 设计令牌单一来源（tokens.css + Tailwind @theme，禁裸值）；组件查找序 beUI→shadcn 生态→手写（beUI 动效参数照抄移植 Vue）；基元内聚住 packages/ui；图标只用 Lucide（manifest 存图标名，拒绝 emoji）；中文写死、亮色单主题、系统字体 | requirements.md §6 #18 |
| #19 | 2026-09-06 | 手机原则 | 平台级要求：一切界面 mobile 可用 = 能看 + 轻操作；重编辑/重管理允许桌面增强（按操作深度分不按功能分）；工作台壳双形态：桌面左栏 / 窄屏底部标签栏（同一 nav 数据）；PWA 安装化在 M6 | requirements.md §6 #19 |
| #20 | 2026-09-10 | 身份与门户 | 内置账号为默认登录（密码表单 + SSO 按钮并存，按实例配置显隐）；已有 OIDC 账号首登自动建档复用（JIT，核心只存 issuer+sub 映射）；内置用户密码哈希存 core（2026-09-10 修订） | requirements.md §6 #20 |
| #21 | 2026-09-06 | setup 动线 | 仅部署输出的一次性链接可配置；激活即成管理员并直接进工作台；已激活后 /setup 访问一律重定向，页面不复存在 | requirements.md §6 #21 |
| #22 | 2026-09-06 | 可观测性 | 错误三层透传：成员只见人话+request id；管理员登录态可展开技术详情；服务端 wrangler tail + audit_log，request id 串联排查 | requirements.md §6 #22 |
| #23 | 2026-09-13 | 邮箱可选 | 身份轴与邮件轴正交；邮件轴关闭时批准只翻状态（无开户无发信），内置模式批准即激活；实例特定值（域名/发件地址/API key）= 运行时配置（/admin/settings mail 段，即改即生效免重部署）（2026-09-13 对齐实现） | requirements.md §6 #23 · SPEC §6.6「邮箱域名」（互为补充，SPEC 侧独有措辞：存 core 库 instance_config；API key 保管在 wrangler secret，运行时经设置页读写（读出即 masked）） |
| #24 | 2026-09-13 | 邀请激活 | 邀请链接一次性/限期（默认 7 天）；批准 → 开户（随机初密）+ 后台尽力发信（发信失败不阻塞审批）；激活必经通道=新人凭邀请链接回邀请页三态自助（待审批/设置邮箱密码/去登录），claim 原子重签（单一有效链接、明文不落库）；不经管理员转交；内置无邮件模式批准即激活——注册时设密码（2026-09-13 #134 修订） | requirements.md §6 #24 · SPEC §6.6「新人凭据」（互为补充，SPEC 侧独有措辞：批准=开户+签激活令牌；邮件后台尽力投递（非必经）；邀请表单收个人邮箱字段） |
| #25 | 2026-09-06 | 账号移除 | 禁用 = 摘 authenticate 权限位（数据保留、可逆）；删除只在链路 6 完整版（导出后可选）出现 | requirements.md §6 #25 · SPEC §6.6「移除成员」（互为补充，SPEC 侧独有措辞：数据原地保留） |
| #26 | 2026-09-06 | 通知承载 | 通知类型表（type/文案模板/渠道位），类型是数据不是代码；模块经 SDK 触发为后续扩展路 | requirements.md §6 #26 · SPEC §6.6（同结论，措辞微差：模板/文案模板、后续路/后续扩展路） |
| #27 | 2026-09-06 | 入职门型 | 邀请制唯一入口：一次性+限期链接（默认 7 天，1-365 可调）；批量门（限次限时）为扩展位；永久链接与开放注册不做 | requirements.md §6 #27 |
| #28 | 2026-09-06 | 双门语义 | 邀请链接管「谁能填表」，审批管「谁能进门」；注册表单（内置模式含用户名+密码）入库即 pending，批准即 active | requirements.md §6 #28 |
| #29 | 2026-09-06 | 密码两用途 | 登录密码与邮箱密码是两个密码；合一只在 Stalwart 兼任 IdP 时巧合成立，非产品承诺；忘记密码 = 管理员手动重置（无邮件实例唯一恢复路径） | requirements.md §6 #29 |
| #30 | 2026-09-06 | 用户名唯一性 | 注册时软校验（users+invites 双表查重即时反馈）；批准时硬校验（撞名走可恢复冲突：invite 留 pending + 人话提示） | requirements.md §6 #30 |
| #31 | 2026-09-13 | 发信定位 | CF Workers 出站 TLS 平台不可用（2026-09-13 定案，证据档案 issue #127）：服务端发信=后台尽力增强、失败不阻塞审批；激活交付=邀请页三态自助（决策 #24） | requirements.md §6 #31 · SPEC §5.7 |
| #31 | 2026-09-10 | 模块三分法 | 功能模块（原生 Worker）/ 适配器（包一层外部系统）/ 伴生服务（整台外部服务只做配置接线）；开发、部署、信任边界各异 | requirements.md §6 #31 · SPEC §3 |
| #32 | 2026-09-06 | 官方改编纪律 | fork 只为省时间；默认不跟上游、看情况选择性吸收；继承上游许可证；改造按 L0-L3 适配光谱（官方功能模块走 L3 深度重写）；第三方 DX（sdk 文档/脚手架）是产品面 | requirements.md §6 #32 |
| #33 | 2026-09-06 | 装配交互 | 正式部署路径 = 交互式 CLI：缺 token 打印深链接（权限预填）→ 域名三选（workers.dev 显式第一选项）→ 模块确认 → 九步进度 + 失败三要素（原因/归属/修复）→ 收尾下一步指引；幂等重跑 | requirements.md §6 #33 |
| #34 | 2026-09-06 | 升级迁移 | 已入主的迁移文件永不修改；schema 演进一律新增 `000N_*.sql`；`migrations apply` 幂等收敛；替代 M0 直改 init.sql 做法 | requirements.md §6 #34 · SPEC §5.5 |
| #35 | 2026-09-07 | 邮箱开户抽象 | MailProvisioner 四方法接口（createAccount/disable/enable/resetPassword）住 contracts；Stalwart JMAP 是第一个实现，后续团队可接 Mailcow 等 | SPEC §6.6 |
| #36 | 2026-09-07 | 管理通道凭证 | Stalwart API key（Bearer，最小权限集），wrangler secret 保管，运行时经 /admin/settings 读写（读出即 masked）；不用人账号 Basic | SPEC §6.6 |
| #37 | 2026-09-07 | 应用密码 | M1 = 引导页（跳转 Stalwart 自服务门户）；「邮箱凭据页」用户 token 代调 = A2，排 M6 | SPEC §6.6 |
| #38 | 2026-09-07 | 审批流 | 单级审批，任何管理员可批拒；桌面管理页主入口，手机同页响应式 | SPEC §6.6 |
| #39 | 2026-09-07 | 管理界面位置 | shell 内 admin 路由（角色守卫），apps/admin 目录预留独立台 | SPEC §6.6 |
| #40 | 2026-09-07 | M1 验收形态 | 三形态：内置+无邮件（默认路径全程）/ OIDC+无邮件 / OIDC+Stalwart（真邀一人全程不碰 Stalwart 后台，真实投递） | SPEC §6.6 |
| #41 | 2026-09-06 | 跨模块协作三原语 | 核心代调与 act claim（双主体 token）+ 核心通用 ACL（资源级权限）+ SDK AI 能力（ai.complete()，Provider 配置归核心） | SPEC §5.6 |
| #42 | 2026-09-09 | 第三方模块发布契约 | 模块作者不部署只发布，部署永远是实例侧动作；信任无分级（写部署脚本的人 = 信任决策者）；主题与部署解耦（模块产物只含令牌名，值运行时下发） | SPEC §5.5.1 |
| #43 | 2026-09-09 | 主题令牌契约边界 | 语义令牌清单发布后只增不改名、不删项；动效不在契约内（归模块作者自治，平台只保证静态视觉一致；动效令牌仅为平台内部默认值） | SPEC §6.5.2 |
| #44 | 2026-09-10 | 产品定位 | Unself 交付壳/契约/装配器三样；模块是载荷不是产品；能力轴模型（每项能力一根轴，壳+契约是常量、模块是变量） | SPEC §0 |
| #45 | 2026-09-07 | 贡献协议 | M0 阶段用 DCO（sign-off 即可）；CLA 仅在未来需要商业双重授权时再议 | SPEC §6.6（尾部孤立条目） |
| #46 | 2026-09-15 | CORS 不配置 | 同源部署（壳与模块同域，模块走 `/m/<id>/*`），浏览器同源策略即兜底；主动配 CORS 反而扩大暴露面。将来支持 `runtime: external` 外部模块时再按白名单显式配置 | M1 复核 S9 |
| #47 | 2026-09-15 | 反点击劫持 | 外壳 HTML 加 `Content-Security-Policy: frame-ancestors 'self'` + 基础 CSP（`default-src 'self'`），`X-Frame-Options: SAMEORIGIN` 兼容旧浏览器；模块 iframe 页面由模块 worker 自负（本波未加，登记已知限制） | M1 复核 S8 |
| #48 | 2026-09-15 | Stalwart 终态自述不再独立核验 | 复核环境无 Stalwart 管理凭据，终态只能采信服务端自述；用户认可不再投入（真要独立核验 = 重装实例）。此项**取消**，非推迟 | M1 复核 X2 |
| #49 | 2026-09-15 | 里程碑 tag 约定 | 每个里程碑收官打 annotated tag `v0.<里程碑号>.0`（M0=0.1.0、M1=0.2.0），带 `Signed-off-by`，指向收官终态 `main`；package.json 不跟踪产品版本，tag 是「哪一版生产在跑」的唯一权威；README「版本状态」随 tag 同步 | M1 收官审计（M1 无 tag 收尾暴露的流程缺口） |
| #50 | 2026-09-15 | EdgeChat 搬运路线 | 放弃「L3 深度重写/按规格重写不复制」，改为固定 commit 搬运 + 最小适配；不做 GitHub fork，出处与复现 = third_party/components.yaml 登记（上游 https://github.com/aozorae/Edgechat + 基准 commit 29978c221ee3ae641ce0b9b97851656c00714a5d，2026-09-15 v2.7.0）；GPL 面积 = 含上游代码的 worker 衍生件归 GPL-3.0（modules/chat/worker），前端自研归 AGPL（口径修订自 #10，#10 原文不动）；存储收口豁免 = chat 专属 D1/KV/R2（chat- 前缀）由装配器直接供给，不经 modules 库/SDK 收口 | issue #215（用户拍板 2026-09-15） |
| #51 | 2026-09-16 | WS token 续期语义（chat 模块） | 已建连 socket 的续期 = **控制帧换绑**：SDK 静默续期拿到新 token 后客户端经 WS 发 `{type:'token_refresh', token}`，DO 当场验签（core JWKS、aud=chat）+ JIT 建档 + 房间授权，原子换绑（serializeAttachment）并回 ack `{type:'token_refreshed'}`；新 token 无效（错签/过期/错 aud/停用）→ policy violation 1008 关闭，客户端重连带新 token 建连。**按消息重验保留**：每条消息帧先重验当前绑定（exp 未过 + 用户未停用 + 房间授权），旧 token 过期未 refresh 则下一帧自然 1008——不引入附加宽限窗口，短时效仍为唯一吊销上限。否决候选：per-message 双 token 窗口与过期宽限窗口都让旧 token 多活一段，与「10 分钟短时效是主要吊销手段」（architecture 模块身份节）冲突。实现：modules/chat/worker/src/do/ChannelRoom.js（#217） | issue #217（三方验证：签发侧 services/core-api/src/token.ts@2ab19ef 源码 + wrangler dev 实测 /tmp/tasks/evidence-217-ws-renewal.md + chat-ws-renewal 单测） |
| #52 | 2026-09-16 | 已读回执口径与上报通道（chat 模块） | 逐条已读回执（flows.md 链路 2 落地）：表 `read_receipts(message_id, user_id, read_at)` 主键(message_id,user_id)=幂等锚点，与 `message_reads` 频道级游标**并存不互改**。**口径定案**：`已读 n/m` 的分母 = 该消息**可达收件人** = 房间成员（成员∧未删∧active，访客/停用不计）− 发件人（**发件人恒已读，不计入分母**）；DM 分母=1，已读即 ✓✓。**上报走 HTTP**：`POST /api/messages/read` 批量端点（≤200/批，INSERT OR IGNORE 幂等，未入房成员 403），不走 WS 上行——可见性检测批量触发 + 幂等重试语义天然贴合 REST；socket 只收广播。**推送=聚合广播**：ChannelRoom 新增内部路由 `/receipts`（verified internal，上游广播机制零改动），实际新增行>0 才广播一帧 `{type:'read_receipts', messageId, userId, readAt, messageIds[]}`（批内合并防刷屏，帧协议与上游同族 protocolVersion:1）；幂等重放=零新增=零事件。**补拉=分页内联**（定案理由：消息分页本就是读放大面，内联一次 IN 查询零额外往返；按需拉端点要前端二次编排且离线补齐场景多一跳——否决）：GET /api/messages 每条消息带 `readReceipts{count, readBy[], total}`，另留 DO `/receipts/snapshot`（最新30条聚合）兜底。前端可见性检测（IntersectionObserver）触发上报，token 静默续期经 WS 控制帧换绑（决策 #51）。实现：modules/chat/worker/src/data/read-receipts.js、api/messages.js、do/ChannelRoom.js（#220） | issue #220（红灯证据 /tmp/tasks/evidence-220-worker.md：M1 去重变异/M3 分母变异/M4 推送幂等变异/M5 空批守卫变异，均红→还原复绿） |
| #53 | 2026-09-17 | 安装器形态与实例布局 | 主入口 = 本地 Web 向导（薄壳 `npx @unself/installer`），CLI 降为逃生门/CI；**零克隆**（不要求 clone 仓库、不要求 pnpm install）；实例 = 用户选定目录下 `unself/`（`unself.config.jsonc` + `unself.lock` + `generated/`），`~/.unself/instances.json` 只记「名字→路径」（可重建、不含秘密）；**实例路径在 CLI 与 Web 均须可见** | issue #242 · 修订清单 A 区 |
| #54 | 2026-09-17 | 部署目标模型（placement） | 一个实例 = 一个**逻辑** core（物理副本随意，状态全在库）；模块落点内部两轴：`provisioned`（target: cloudflare\|docker）/ `connected`，对外只暴露一个 `mode: cloudflare\|docker\|connect`（实例级默认、模块级可覆盖）；纯 CF / 纯 Docker / 混杂 = 同一模型的取值 | issue #242/#247 · 修订清单 A 区 |
| #55 | 2026-09-17 | 模块数据四级 | `core`（默认，经 Core API 代理，只提供 get/put/delete/list）/ `shared`（共享 modules 库自建表，零隔离）/ `dedicated`（装配器供给独立 D1/KV/R2）/ `external`（模块自备外部库）；模块声明 `storage.accepts` + `preferred`，**由用户在安装时选**，选了 `accepts` 之外的模式即拒绝安装；`shared` 需三护栏（独立迁移记账表 / `tables` 清单申报 / 命名与跨模块外键硬校验）+ 安装时知情同意 | issue #248 · 修订清单 A 区 |
| #56 | 2026-09-17 | 模块权限模型 | 契约新增 `permissions` 作为**门禁**：词表由 core 定义并随契约版本走，模块 token 带对应声明，Core API 按能力放行/403；**未知能力安装时直接拒绝**（沿用 #64 已立原则「静默丢会致 token 授牌错误」）。同时**删除 `capabilities`**（放错层：模块提供什么功能不属 core 契约）与 **`requires`**（恒定值、无消费者） | issue #243 · 修订清单 A 区 |
| #57 | 2026-09-17 | 契约版本与发版约定 | 契约版本（`CONTRACT_VERSION`，独立于产品版本）**仅在破坏性变更时 bump**；增量变更（新字段有默认值、新能力入词表、新端点）天然不拦人；`compat` **可选**，省略 = 接受任意版本；注册表记「安装时的契约版本」，core 升级时据此发现潜在破坏并停用 + 提示（`--allow-incompatible` 可硬上）；**发版约定（破坏性定义、弃用周期、bump 责任、CI 卡点）留档待定** | issue #243 · 修订清单 A 区 |
| #58 | 2026-09-17 | 模块来源与包契约 | 来源协议：`official:` / `npm:`（含私有 registry）/ `github:` / `https:` / `file:`；**远端一律要求已打包**——安装器直接从 registry 取 tarball 解包、**不走 npm install**（`postinstall` 无执行机会）、**不执行任何第三方构建脚本**；仅 `file:` 允许本地源码 + 本地构建（打包器按需加载）；包格式冻结为 `manifest.json` + 预构建 `worker.js` + `assets/` + `migrations/` + `config`/`tables` 声明 | issue #245 · 修订清单 A 区 |
| #59 | 2026-09-17 | 模块身份 | **全局唯一 = 包名/来源**；`manifest.id` = 作者建议的**实例内名字**，安装时可改；撞名由安装器提示改名；注册表主键仍是本地 id，升级靠 `source` 定位包 | issue #243/#245 · 修订清单 A 区 |
| #60 | 2026-09-17 | 模块依赖与完整性 | worker 模块依赖**全部 bundle 进 `worker.js`**（零运行时安装、无动态装包）；docker 模块 manifest 写 tag、`unself.lock` 钉 **digest**（多架构钉 manifest list）、按 digest 部署、私有 registry 凭据走 secret 不落 config/lock；`unself.lock` 用 SRI `sha512` + manifest 哈希；重跑用 lock、升级显式、哈希不匹配直接拒绝；builtin 也进 lock；**SDK 两分**——浏览器侧 SDK 为平台资产由安装器注入（与 core 同版本），worker 侧 SDK 属模块普通依赖随 bundle | issue #245 · 修订清单 A 区 |
| #61 | 2026-09-17 | 迁移契约 | 迁移文件必须**逐条幂等**（记账表只在整份成功后记账，失败重跑整份重放）+ **只写增量安全语句**（`CREATE` / `ADD COLUMN` / `CREATE INDEX`，破坏性变更走 expand/contract）；二者进 `unself module validate` 静态检查；失败时停住并指出「模块 / 文件 / 语句」，**不自动重试、不自动回滚**（D1 无事务）；**不做**迁移前自动备份与破坏性确认门（低频，复杂度不划算） | issue #248 · 修订清单 A 区 |
| #62 | 2026-09-17 | 升级与回滚 | 平台升级（`unself upgrade`）与模块升级（`unself module upgrade <id>`）**分开**；升级前先算影响面（哪些模块 `compat` 会落空）并列 diff 确认；每次变更前把旧 `unself.lock` 存一份到 `generated/history/`（兼审计）；回滚 = 手动拷回快照重跑（**不做**专门回滚命令 / 历史栈 / 自动回滚）；**降级允许 + 警告**（无自动备份时禁止降级等于封死退路）；fail-safe（新容器健康才切旧、CF 覆盖式部署） | issue #248 · 修订清单 A 区 |
| #63 | 2026-09-17 | 跨域与 hybrid | `publicUrl`：CF 模块自动推导；非 CF 模块由用户填且**强制 https**；外壳 CSP `frame-src` **按注册表动态生成**（加模块不必重建外壳），模块页必须带 `frame-ancestors`，Core API 按注册表 origin 白名单开 CORS；**前缀剥离归宿主**——「模块恒挂根路径」为不变式（CF wrapper / 反代 / 独立域名各自负责）；生成 Caddy/nginx 反代片段但不代管用户反代；可达性体检复用冒烟并诚实标注「只证明安装器可达」 | issue #247 · 修订清单 A 区 |
| #64 | 2026-09-17 | 装配器控制面与后端解耦 | 引入 `ControlPlane` 接口（`upsertModule` / `issueSetupToken` / `readRegistry`）：CF 实现直敲 D1、Docker 实现走 `node:sqlite`；SQL 与校验逻辑收在 core 与装配器**共用模块**，两边同一份代码防漂移；迁移记账自实现（按模块独立记账表，落实 #55 的记账隔离）；#165 的本地签发走同一接口 | issue #244 · 修订清单 A 区 |
| #65 | 2026-09-17 | 工具链策略（不依赖 wrangler） | 装配器**不安装、不调用 wrangler**，自建 Cloudflare REST 客户端（installer 回到几 MB，并消除版本漂移）；OAuth 经 `wrangler auth token` 借用（官方文档明确支持 "for use with other tools and scripts"），机器上没有 wrangler 时走 API Token；实测依据：wrangler 完整安装 213MB（npm）/167MB（pnpm），其中 workerd 二进制 147MB **无法剥离**（miniflare 在启动时 require），而仓库 `.git` 仅 21MB | issue #244 · 修订清单 A 区 · docs/audit/241-oauth覆盖实测-2026-09-17.md |
| #66 | 2026-09-17 | 凭证与授权路径 | 认证优先级同官方：`CLOUDFLARE_API_TOKEN` > API key/email > OAuth；**OAuth 为主路径**（浏览器授权、零复制粘贴），**API Token 为折叠兜底**（需要 Total TLS 的多级子域 / 用户偏好 / CI）；token 一律**填在 Web 向导的密码框**（掩码、可校验、可重试），不在终端粘贴；**实测修订**：wrangler **4.129.0 / 2026-09-17** 实测表明 OAuth 覆盖 zone 路由增/改/删 + D1 + R2 + KV + deploy + secret，**仅 Total TLS 不可用**——原「OAuth 对 zone 路由授权不足、必须用 API Token」结论作废 | issue #246 · 修订清单 A 区 · docs/audit/241-oauth覆盖实测-2026-09-17.md |
| #67 | 2026-09-17 | 环境探测与平台差异 | 启动时**只探 2 项**：OAuth 回调可达性（容器 / 远程 SSH / 无浏览器 → 改设备码或引导 token）、TTY；其余（平台、代理、Node 版本、端口占用、已有实例）一律「用到再读 / 失败给人话」，不设环境探测阶段；平台差异收敛进单个 `platform.ts`（3~4 个纯函数），**不做 adapter、不做三平台各一份实现**；靠注入参数在 Linux 上覆盖 Windows/macOS/WSL 分支，CI 保持 Linux | issue #246 · 修订清单 A 区 |
| #68 | 2026-09-17 | 补 #2：一键入口重定案 | #2 记载的「一键入口（Deploy Button + Workers Builds）为路线图」**在当前架构下不可行**：Cloudflare 官方限制 monorepo 多 Worker 不能一并部署、仓库必须 public；且 CF 侧编排与 #165（setup token 只在部署者本地签发）冲突。该路线图项需重定案，本阶段不实现（原条目文字不动） | issue #241 · CF 官方文档（Deploy Button 不支持 monorepo / 需 public 仓库） |
| #69 | 2026-09-17 | 补 #15：装配引擎形态变更 | #15 原文「装配 = wrangler 幂等脚本」在执行层被 #65 取代：装配改为**自建 REST 客户端**（「读 `unself.config.jsonc`、只在部署时执行、实例不持凭证」三条不变）；并新增 #64 的控制面接口使 core 落点无关 | issue #244 · 修订清单 A 区 |
| #70 | 2026-09-17 | 补 #33：交互主入口变更 | #33 记载的六段交互流保留，但**载体由「终端内 CLI」改为「本地 Web 向导」**（#53），CLI 保留为逃生门/CI；六段流语义（缺凭证给深链接、域名选择、模块确认、进度 + 失败三要素、收尾下一步、幂等重跑）不变 | issue #242 · 修订清单 A 区 |
| #71 | 2026-09-17 | 补 #14：数据归属扩展为四级 | #14 的「单一 modules 库 + 表前缀 + SDK 查询收口」保留为其中的 `shared` 一级，整体扩展为四级（#55）；`shared` 需补三护栏与知情同意 | issue #248 · 修订清单 A 区 |
| #72 | 2026-09-17 | 补 #42：第三方发布契约落地形态 | #42 的「模块作者不部署只发布、信任无分级」不变；补上落地形态：来源协议、包格式、身份规则（#58/#59）、完整性（#60），以及「远端不执行第三方构建脚本、只有 `file:` 本地目录允许构建」的信任边界 | issue #245 · 修订清单 A 区 |
| #73 | 2026-09-17 | 补 #46/#47：跨域白名单启用 | #46 原预留「将来支持 external 外部模块时再按白名单显式配置」——本轮启用：外壳 `frame-src` 按注册表生成、Core API 按注册表 origin 开 CORS、模块页必须带 `frame-ancestors`（#63）；#47 对外壳的反点击劫持不变，模块页因其自身 CSP 归模块作者（本轮改为强制要求） | issue #247 · 修订清单 A 区 |
| #74 | 2026-09-17 | 补 #50：chat 存储豁免普通化 | #50 的「chat 专属 D1/KV/R2 由装配器直接供给」不再作为硬编码特例，改为 #55 中 `dedicated` 的**普通实例**（manifest 声明 `storage.accepts` 含 `dedicated` + `tables` 清单），装配器侧去掉按模块 id 的分支 | issue #248 · 修订清单 A 区 |
| #75 | 2026-09-18 | 补 #56/#55：模块后端面端点落定 | 权限门禁要能验证「未声明即调用对应 Core API → 403」，就必须有真实的 Core API 面，故随 #243 一起落地**模块后端面** `/api/module-api/*`：`storage` 四形状（get/put/delete/list）走 Core API 代理、`notify` 走站内投递（`module_notify` 通知类型随 core 迁移 0008 种入——通知类型是数据不是代码）；`acl`/`ai`/`realtime`/`mail` 四个词门禁生效但端点 `501` 预留。**收敛责任归 #248**：module-sdk 的存储客户端必须收敛到该代理，禁止「SDK 直连 D1」与「Core API 代理」两条并存（T4/T5 审计吃过「两张皮」的亏） | issue #243 · docs/modules.md §9 |
| #76 | 2026-09-19 | 发布渠道与包清单（npm 真实发布） | 对外只发 4 个包（顺序即发布顺序）：`@unself/sdk` → `@unself/hello` → `@unself/chat` → `@unself/installer`。**不发**：`@unself/contracts`（内部包，对外可见部分由 SDK 具名导出）、引擎代码 `@unself/deploy-cloudflare`（构建期 bundle 进 CLI）、平台产物（core worker bundle / 壳 / 迁移 SQL / vendor）——**保持内嵌**，理由：唯一消费者就是安装器；Docker 形态落地后再评估是否抽包。**本决策替代 #257 中「内置模块随安装器内嵌分发」的那部分**：官方模块改由安装器 `dependencies` 精确版本预装 | issue #282 · 本波发布 |
| #77 | 2026-09-19 | 补 #58：删除 `official:`，官方模块与第三方同等 | 来源协议五收敛为四：`npm:` / `github:` / `https:` / `file:`。官方模块就是普通 npm 包（`@unself/hello`），**没有特权来源通道**——与 #42「信任无分级」一致（安装、权限门禁、卸载一视同仁）。**A 方案**：配置文件里也写 npm 串（`"npm:@unself/hello@0.1.0"`），代码中不保留 builtin 来源分支；`npm:` 解析语义 = **本地 `node_modules` 有该包且版本匹配则直接用（零网络），缺了才去 registry 取**，官方模块由此获得「随安装器预装、离线可用」，但这是**默认值不是特权** | issue #284 · 替代 #58 的 `official:` 部分 |
| #78 | 2026-09-19 | 补 #42/#60：模块分发形态与 SDK 收敛 | ① 模块的 npm 包 = **可安装包**（`manifest.json` + 预构建 `worker.js` + `assets/` + `migrations/` + `LICENSE`/`NOTICE` + 生成的 `package.json`）；`unself module pack` **直接产出可 `npm publish` 的包**（生成 `package.json`：name/version/files），安装侧真相仍是 `manifest.json`——**修订 docs/modules.md §2 排除 `package.json` 的旧禁令**。② `@unself/sdk` 是模块作者**唯一**要装的包：`@unself/contracts` 保持内部，其对外可见部分（manifest schema、主题令牌名、契约版本、权限词表）由 SDK 具名导出、按需 tree-shake | issue #285 · docs/modules.md §2 |
| #79 | 2026-09-19 | 发版约定（补 #57 留档待定项） | ① **tag 即版本**：`installer-v0.2.0` / `sdk-v0.1.0` / `hello-v0.1.0` / `chat-v0.1.0`，发布时从 tag 截出版本号写进 `package.json`（不做一致性断言——tag 即真值，不可能对不上）。② 四个包**各自独立发版**。③ 发布流水线**只做** checkout → install → build（产制品）→ `npm publish`；**不重跑 CI**（PR 阶段六步门禁已过）。④ 首次发布由维护者本机 `npm login` 手工发一轮；之后在 npm 包设置里配 **Trusted Publisher（GitHub Actions OIDC）**：仓库 `UN-Self/unself` + workflow 文件名 `release.yml`，**须显式勾选允许 `npm publish`**（2026-09-03 之后新建的配置默认只允许 `npm stage publish`）。⑤ provenance 由 Trusted Publishing **自动生成**（不需 `--provenance` 参数）。⑥ 仓库**不存任何 npm token/secret**。⑦ 回滚 = `npm deprecate` + 发补丁版；unpublish 仅限 72h 内且无依赖。⑧ **发版前提**：tag 在 `main` 上，且发版前有一次真机走查记录 | issue #286 · 关闭 #57 的「待定」 |
| #80 | 2026-09-19 | 版本可见性 | 安装器与平台产物内嵌**构建时的产品版本与 commit SHA**（写进产物 manifest），`unself --version` 与每次部署收尾屏打印；目的是「线上到底跑的哪一版」可自证。管理台检测 npm 最新版属可选后话，本轮不做 | issue #287 |
| #81 | 2026-09-19 | 模块热更边界 | 模块可热更（模块 Worker 重新上传，**core 不停机**）。三条边界必须进文档：① 已打开页面需刷新才拿到新资产（资产哈希变化）；② 模块迁移必须向后兼容（记账按文件名，只有新增安全）；③ 契约版本或权限声明变更时，核心须在装配期**重新签发 module-token**（不需重启核心）。**注**：今天 CLI 只有整体 `unself deploy`；「只更一个模块」的命令（`unself module update`）**待定**，属新需求 | issue #285 · docs/modules.md |
| #82 | 2026-09-19 | 模块作者支持边界（只给能力，不给框架） | 对外只提供：① `@unself/sdk`（装了就能用）；② 契约文档（manifest 字段 / 权限词表 / 主题令牌 / 握手与 API）；③ 两条命令 `unself module validate` + `unself module pack`；④ 参考实现 = `hello` / `chat` 的公开源码。**不做**：脚手架与模板生成器、开发框架、dev server 或本地联调工具链、兼容层与抽象层 | issue #282 · 头脑风暴定案 2026-09-19 |
| #83 | 2026-09-20 | 仓库两桶判据（#303 目录整理） | **判据 = 谁看得见**：`core/` = **开发用的代码依赖**（库：contracts / sdk / ui / control-plane / adapters）；`app/` = **开发好的 app，可被装配**（installer / workbench / modules，全部发 npm）。改名表：`packages/*` → `core/*`；`adapters/**` → `core/adapters/**`（保持 `{mail-smtp, provisioning/stalwart}` 层级）；`packages/installer` → `app/installer`；`modules/*` → `app/modules/*`；`services/core-api` + `apps/shell` → `app/workbench`；引擎 `deploy/cloudflare` → `app/installer/src/engine`。`third_party/` 留顶层（合规台账，非代码）。**288 个 `git mv` 保历史、零语义改动** | issue #303 · PR #304 |
| #84 | 2026-09-20 | `app/workbench` = shell + core-api 合并（一个包两半边） | `src/` 后端（core Worker）+ `web/` 前端（工作台 SPA）+ `migrations/`；一个 `package.json`、两条构建（esbuild → `dist/worker.js`；vite → `dist/web`）。**Worker 名 base 由 `core-api` 改 `workbench`**：默认 `<ns>-workbench`（历史 `unself-core-api`）——破坏性变更已获批准：**删旧建新，D1 / R2 / KV 名一律不动**（改名即丢数据，所以不改的是数据那侧）。`/api/health` 自述 `service: 'workbench'` | issue #303 · docs/deploy.md「升级已有实例」 |
| #85 | 2026-09-20 | 平台产物随 `@unself/workbench` 发布（**修订 #257 口径**） | 产物 = workbench 包内的预构建物（`dist/worker.js` + `dist/web` + `migrations/`）；安装器**只当消费者**：从 `node_modules/@unself/workbench` 解析（顺序：显式目录 > 包解析；与官方模块同一条 `localPackageDir`，#284 单一实现）。**废弃**：安装器内嵌 `dist/artifacts/**`、以及「仓库形态现构建 / 产物照抄」两套行为。生产组合根（createApp + Stalwart 注入 + 资产回退 + 帧白名单）从生成的 JS 入口模板搬进包内真源文件 `src/entry.prod.ts`——部署工具不再需要知道 app 的内部文件布局 | issue #303 · 修订 #257 |
| #86 | 2026-09-20 | 第五个发布包（**修订 #76**）+ 发布顺序 | 发布面 4 → **5 个包**：`@unself/sdk`（core）；`@unself/workbench` / `@unself/installer` / `@unself/hello` / `@unself/chat`（app）。**顺序：workbench 必须先于 installer**（后者依赖前者取产物）。workbench 发布形态 = 预构建产物（`files: ['dist','migrations']`）+ **零 `dependencies`**（运行期依赖全进 bundle / 前端资产）；版本仍以 tag 为准 | issue #303 · 修订 #76 / #79 |
| #87 | 2026-09-20 | 测试不得依赖目录与包名 | 测试测行为，行为不因目录改名而变。断言里不写仓库相对路径、包名、目录层级；定位仓库内资源按**包名 / 结构**解析（向上找 `pnpm-workspace.yaml`；范本 `app/installer/test/helpers/repo-root.ts`）。**验收 = 改名 / 搬目录后行为类断言零变化**。反例（#303 实测）：20 处 `new URL('../../../..')` 硬算在搬目录后**不报错只算歪**，报出「模块目录不存在」这种假病因 | issue #303 · docs/testing.md |
| #88 | 2026-09-20 | 结构闸门 `verify-paths`（目录类错误在结构层抓） | `scripts/verify-paths.mjs` 三条断言，接进 CI 第 3 段：① 每个 tsconfig 的相对 `extends` 可解；② `pnpm-workspace.yaml` 的 globs ↔ 含 `package.json` 的目录一一对应；③ 代码里不得再出现已删顶层目录的路径写法。**理由**：#303 当天 4 处 `extends` 断链**只有 build 抓得到，行为测试抓不到** | issue #303 · PR #304 |
| #89 | 2026-09-20 | 审计产出进 issue + PR（工作规矩） | 实测证据 / 体积 / 用例数 / 事故与清场，一律贴到对应 issue 与 PR，**本地不留**（`/tmp` 里的临时报告不是交付物）。写成「结论 + 证据」，够别人自己复现即可——**别写成流程剧本**（规矩要薄，防照本宣科） | 用户拍板 2026-09-20 |
| #90 | 2026-09-20 | 补注：早前条目里的旧路径（不改原文） | #18「基元内聚住 packages/ui」→ 今 `core/ui`；#39「apps/admin 目录预留独立台」→ 现状是 `app/workbench/web` 内的 admin 路由；#51 实现文件「modules/chat/worker/src/do/ChannelRoom.js」→ 今 `app/modules/chat/worker/src/do/ChannelRoom.js`。**只增不改：以上面几条 + 本条为准** | issue #303 |
| #91 | 2026-09-20 | 默认主题包换值：暖纸黛墨（#307） | `bg #faf9f7`（暖纸）/ `surface #ffffff`（卡面浮起）/ `surface-hover #fafaf9` / `surface-active #f5f5f4` / `primary #4338ca` / `primary-hover #3730a3` / `primary-soft #e0e7ff`；text/border/状态四色/scrim 不动。动机：design critique（快照 2026-09-20，Nielsen 30/40）判定「视觉皮肤任意产品可套用」，登录页主色原为 Tailwind 默认蓝。语义清单名不变，只换值——模块零改动 | issue #307 · 用户拍板 2026-09-20 |
| #92 | 2026-09-20 | 动效数值骨架进契约（**修订 #43/#95 口径**） | duration/ease/press-scale/lift-y 等**数值**入语义令牌清单（含 #191 基元动画时长一并收编）；**组合用法仍归模块作者自治**（平台不评审用法）。理由：安装器无构建（node:http 内联页）无法 import core/ui，`theme-tokens.json` 是与工作台共享取值的唯一通道；SetupView 内联三段重复 linear() 样板即漂移实证。全部动效仍须带 reduced-motion 降级。specKeys 守卫同步扩容 | issue #307 · 用户拍板 2026-09-20 |
| #93 | 2026-09-20 | 安装器向导化六步 + zone 自动发现 + 模块配置页（#53/#66 落地） | ①凭证（OAuth 检测→主路径直跳）→②域名（workers.dev 默认 / 账户 zone 自动列出、只填子域前缀）→③模块→③★模块配置（**每模块独立一页**，manifest.config 声明驱动，无 config 自动跳过）→③½数据存放→④装配（九步进度条）→⑤完成（peak-end：绿勾+激活链接+摘要）。secret/config 值只进程内存（同 #272 token 语义）；连接测试 M1 仅 `http`，其余装配时验证。落地口径见 docs/modules.md §5 / docs/deploy.md | issue #307 · 用户拍板 2026-09-20 |
| #94 | 2026-09-23 | 文档治理三则 + M2 状态口径（docs 全量对齐） | ① **M2 进行中**（里程碑未关单：8 open + #306；不打 v0.2.x tag）；② **README 不写「版本状态」节**（作废机制⑤第 4 条的该要求）；③ **验收记录只存对应 issue/PR，禁止落成文档**（`docs/` 下不得有验收/审计记录类文件；`docs/audit/*` 一并删除，正文迁入对应 issue 评论）——与 #89 同源，进一步收口到「不得落成文档」；④ docs 全量对齐（architecture / flows / requirements / testing / PRODUCT_SPEC / ai / roadmap）。**补注**：#65/#66 来源列指向的 `docs/audit/241-*` 已删除，记录迁入 issue #241（只增不改，不回溯改原文） | issue #317 · 用户拍板 2026-09-23 |
