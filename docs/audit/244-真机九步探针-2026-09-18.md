// SPDX-License-Identifier: AGPL-3.0-only
# #244 真机九步探针实测（2026-09-18）

分支 `m2/issue-244` · 探针代码 `/tmp/probe244/copy`（worktree rsync + 名称常量补丁，见「探针补丁」）·
API Token 来源 `wrangler auth token`（OAuth，仅探针/测试脚本用，生产零 wrangler 不变）。

## 探针补丁（与 worktree 的差异，仅命名/注入）

1. 名称常量：`unself-core`/`unself-modules`/`unself-core-api`/`unself-module-<id>` → `unself-probe-244-*`（5 处文件、纯常量替换）
2. `probe-run.mts` 注入（不改产品语义）：
   - `ensureDns`：探针域名 A 记录自建（OAuth 无 `dns_records` 权限，等价部署者手动建 DNS；workers.dev 模式实际未触发 zone 路由路径）
   - `fetchJwks` / smoke `http`：本机 `*.workers.dev` DNS 污染（解析到 Facebook IPv6），用 https 模块钉 CF IP `104.21.0.100` + SNI/Host 直连——产品代码默认 fetchJwks 不变，正常网络无需钉扎

## 真机确认的 REST 契约（修入代码，均有红灯/断言）

| 契约 | 实测 | 修复 |
| --- | --- | --- |
| assets manifest key 必须 `/` 开头 | 违反 → `10304` | `buildAssetManifest` 加前缀；cf-rest-fake 校验 |
| asset hash = blake3(base64+ext) hex 前 32 位 | 10 字符截断 → 资产 500（hash 寻址不中） | `assetHash` 对齐 wrangler hash.ts；断言 `/^[0-9a-f]{32}$/` |
| assets-upload-session 需 `Authorization: Bearer` | 无头 → `9106` | `RestClient.token` getter + 请求头 |
| assets-upload-session 响应 = 信封 `result:{jwt,buckets}` | 2026-09-17「裸响应」结论有误 | `json?.result ?? json` 兼容 |
| 批量上传成功 = HTTP 201 信封 `result.jwt` | 裸 `{jwt}` 亦兼容 | `json?.jwt ?? json?.result?.jwt` |
| putWorker metadata 需 `assets:{jwt,config}` | 缺失 → `10305` "Cannot use an assets binding without assets" | `WorkerUpload.assets` |
| assets config 需 `html_handling` | wrangler 默认 auto-trailing-slash | `htmlHandling`（默认同值） |
| REST 化后 core 入口需自打包 | wrangler deploy 隐式 bundle 消失 | `bundleCoreWorker`（esbuild，入口模板写盘后调用） |
| import 已见 etag | 嵌套 `status:'complete',result:{final_bookmark,num_queries}` | d1.ts 视为已完成（此前已修） |
| settings GET 404（10049）= 新 worker | `isWorkerNew` 判定 | DO migrations 首部署注入 |

## d1 import 首应用语义复核（此前疑点）

干净库首跑：init（新 etag）→ `{upload_url, at_bookmark}` → 上传 → poll → complete；
同 etag 二次 init → 嵌套 complete。九步二跑日志「迁移已应用」与 core 表齐全一致，疑点关闭。

## 九步真机结果（`unself-probe-244-*` 全家桶）

```
①D1 core/modules 创建（886a739a-… / bcc9706e-…）
②迁移 core 7 条 + hello 2 条（独立记账 unself_migrations_core / _hello）
③Shell 上传（assets 23 文件）
④hello 上传 + /m/hello/* workers.dev 路由；JWKS 抓取注入
⑤registry upsert hello=1；chat disable
⑥R2 unself-storage 复用（生产桶名，见「误伤」③）
⑦OIDC 跳过（setup 向导录入）
⑧setup token 签发/复用（幂等）
⑨smoke：core /api/health ✓；hello /m/hello/api/health ✓（模块自身 workers.dev 域）
exit=0；二跑幂等（零创建、token 复用）
```

终验：core 库 `module_registry=[hello,enabled=1]`、`setup_tokens` 未消费、`unself_migrations_core`=7；
modules 库 `_cf_KV` + `unself_migrations_hello`(0001_init,0001_module_kv)。

workers.dev 模式下 `<base>/m/<id>/*` 结构性落回 core SPA（无 zone 路由）——平台事实；
smoke 的模块探活改打模块自身 workers.dev 域（domain 模式不受影响，zone 路由转发）。

## 探针误伤生产（如实记录，已修复）

1. **生产 `unself-module-hello` 被覆盖**（首次探针步骤④未隔离模块 worker 名）：
   modified_on 2026-09-17T18:59:31Z，MODULES_DB 绑定被改成 probe 库 id `bcc9706e-…`。
   **已修复**：用仓内 `modules/hello` 源码同构重打包（esbuild bundle + prefix-strip wrapper +
   sdk 资产），MODULES_DB 绑回生产库 `61636ae5-…`，JWKS 沿用生产原值（kid `KIXP8AAA…`）。
   修复后实测：`/m/hello/api/health` 200 `{"ok":true,"service":"module-hello"}`；
   `/m/hello/` 200 HTML；`/m/hello/sdk/module-sdk.esm.js` 200 JS。
2. **probe JWKS kid 曾注入生产 hello**（随覆盖发生，随修复消除）——生产 core JWKS 未动过。
3. **R2 桶名未加前缀**：探针步骤⑥按 `storage.bucket=unself-storage` 复用了生产桶（只列出，
   未写入对象；chat 未选模块，无数据路径）。生产桶资源完好。
4. 生产 `unself-core-api` / `unself-module-chat` 绑定核对无恙（CORE_DB `d1c3cf32-…`、MODULES_DB `61636ae5-…`）。

## 清理与残留

`cleanup.mjs` 删除 15 项（2 D1 + 13 worker，含调试 worker `-cfg-*`/`-asset-min`）；
五路残留复查全 0：`{"d1":0,"r2":0,"kv":0,"worker":0,"zoneRoutes":0}`（preflight 复核通过）。

## 环境注意

本机 `*.workers.dev` DNS 污染（→ 104.244.46.5/Facebook IPv6），探针脚本以
`https` 模块 host=104.21.0.100 + servername/Host 直连。生产域名 `team.handywote.top` 不受影响。
